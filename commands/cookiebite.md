---
description: Build a polished single-file HTML report with the cookiebite skill
argument-hint: [what to report on, or paste the data]
---
Use the **cookiebite** skill to build a polished, single-file, interactive HTML report.

Request / data:
$ARGUMENTS

Run the cookiebite workflow end to end: choose the right charts and layout, apply the
default theme (or any theme the request names), and do the visual self-check before
handing the file back. If no data was given above, ask what to report on first.
