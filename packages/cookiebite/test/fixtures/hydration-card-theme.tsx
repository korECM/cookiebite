import { Card, CardContent } from '@/components/ui/card';

export const __theme = { seed: { accent: '#112233' } };

export default function App() {
  return (
    <Card>
      <CardContent>hydration-fixture-text</CardContent>
    </Card>
  );
}
